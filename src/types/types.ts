export type FetchedData = {
    id: number;
    title: string;
    image: string
    price: string
}

export type SingleProduct = {
  id: number;
  title: string;
  price: number;
  description: string;
  image: string;
  category: string;
};